# Отчет по домашнему заданию к семинару 6

## 1. Валидатор схемы

Сигнатура:

```python
def validate_plan(plan: Plan) -> list[str]:
```

Разрешенные инструменты:

```python
VALID_TOOLS = {"get_fx_rate", "get_key_rate", "get_inflation", "calculate"}
```

Пойманные примеры:

```text
подвопрос 1: неизвестный инструмент get_cumulative_inflation
подвопрос 2: expected_tools должен содержать только имя, не вызов: get_fx_rate(currency="USD")
```

В `run_pwc()` валидатор вызывается сразу после `planner(...)`. Если есть ошибки,
планировщик вызывается повторно с feedback. Если после feedback план все еще
невалидный или пустой, PWC возвращает честный отказ вместо передачи
галлюцинированного инструмента исполнителю.

## 2. Параллельность

`_topological_sort` заменен на `_topological_levels`: независимые подвопросы
попадают в один уровень. `execute_level(...)` запускает один уровень через
`ThreadPoolExecutor`.

Команда:

```bash
python measure_parallel.py -n 1
```

Текущий прогон через Gemini `gemini-2.5-flash` с throttle 13 секунд:

| Вопрос | sequential | parallel | speedup | валиден |
|---|---:|---:|---:|---|
| Q1 | 52.157s | 51.935s | 1.004x | нет, runtime failure |
| parallel_bonus | 51.857s | 52.022s | 0.997x | нет, runtime failure |

Код параллельного исполнения работает, но на бесплатном Gemini throttle
сериализует сетевые вызовы, поэтому измеримого ускорения нет.

## 3. Замер критики

Добавлен `critic_experiment.py` с 5 заведомо битыми кейсами:

- арифметика без `calculate`
- выдуманное число
- несогласованные данные
- ошибка исполнителя
- план не покрывает вопрос

Полная команда:

```bash
python critic_experiment.py -n 10
```

Попытка полного прогона через OpenRouter `nex-agi/nex-n2-pro:free` показала
важную совместимость: часть free-провайдеров не принимает запросы, где всё
содержимое лежит только в `system`. После этого `critic.py` исправлен: основной
текст проверки теперь отправляется как `user`-сообщение.

Файл `critic_experiment_results.json` сначала фиксировал не ложные принятия, а
ошибки провайдера. После исправления повторить полный N=10 на OpenRouter не
удалось, потому что free-tier исчерпал дневной лимит `free-models-per-day`.

Дополнительно был выполнен прогон через Gemini `gemini-2.5-flash` с throttle.
Текущий `critic_experiment_results.json` содержит результат этого запуска:

| Битый кейс | T=0.0, ложных принятий | T=0.7, ложных принятий | Ошибок API |
|---|---:|---:|---:|
| арифметика без calculate | 7/10 | 2/10 | 4 |
| выдуманное число | 3/10 | 0/10 | 14 |
| несогласованные данные | 0/10 | 0/10 | 18 |
| ошибка исполнителя | 0/10 | 0/10 | 2 |
| план не покрывает вопрос | 2/10 | 0/10 | 17 |

Из-за API-ошибок этот результат нельзя считать идеально чистым полным N=10,
но он показывает тенденцию: при T=0.7 ложных принятий меньше, чем при T=0.0.

## 4. Eval 6x3

Добавлено 6 кейсов и 3 конфигурации:

- одиночный агент С5
- PWC без валидатора
- PWC + валидатор

Команда:

```bash
python eval_pwc.py -n 3
```

Результат текущего прогона:

| id | single | pwc | pwc+validator |
|---|---:|---:|---:|
| Q1 | 0/3 | 0/3 | 0/3 |
| Q2 | 0/3 | 0/3 | 0/3 |
| Q3 | 0/3 | 0/3 | 0/3 |
| Q4 | 0/3 | 0/3 | 0/3 |
| Q5 | 0/3 | 0/3 | 0/3 |
| Q6 | 0/3 | 0/3 | 0/3 |

Прогон `N=3` выполнен полностью. Все конфигурации получили 0/3, что отражает
не синтаксические ошибки, а слабую надежность связки free-tier LLM + строгие
проверки качества ответов. Для `N=5` нужен более высокий лимит API.

```bash
export LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
export LLM_MODEL=gemini-2.5-flash
export LLM_CALL_INTERVAL_SEC=13
python eval_pwc.py -n 5
```

## 5. Вывод

PWC оправдан, когда вопрос естественно раскладывается на независимые факты и
явную арифметику: курсы на разные даты, ставка плюс инфляция, несколько
валютных запросов. Он хуже одиночного агента, когда планировщик начинает
выдумывать tools или когда внешний LLM нестабилен: тогда нужен валидатор,
лимиты на retry и честный отказ вместо попытки любой ценой получить ответ.
